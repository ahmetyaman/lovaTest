import { LangTransPipe } from './lang-trans.pipe';

describe('LangTransPipe', () => {
  it('create an instance', () => {
    const pipe = new LangTransPipe();
    expect(pipe).toBeTruthy();
  });
});
